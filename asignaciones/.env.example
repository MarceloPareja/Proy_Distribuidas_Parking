# ─────────────── SERVIDOR ───────────────
PORT=3002

# ─────────────── BASE DE DATOS (PostgreSQL) ───────────────
DB_HOST=localhost
DB_PORT=5434
DB_USUARIO=asignaciones
DB_CONTRASENA=asignaciones
DB_NOMBRE=asignaciones

# ─────────────── KONG API GATEWAY ───────────────
KONG_BASE_URL=http://localhost:8000

# ─────────────── API KEY (Consumer sistema-tickets) ───────────────
# Usada por VehiculosClient para autenticarse ante Kong (Key Auth plugin).
# NO hardcodear en el código — siempre leer desde variable de entorno.
# En producción: inyectar vía Docker secrets, K8s secrets, o CI/CD.
KONG_API_KEY=your-api-key-here
